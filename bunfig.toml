[install]
# Hoist all packages to the root node_modules, like classic npm behavior.
# This prevents per-package node_modules from being created for workspace
# packages' peer dependencies, solving the expo-doctor duplicate warnings.
linker = "hoisted"
