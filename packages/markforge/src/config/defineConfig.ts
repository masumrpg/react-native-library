import type { MarkforgeConfig } from "./types.js";

/**
 * Type-safe configuration helper for markforge.config.ts
 */
export function defineConfig(config: MarkforgeConfig): MarkforgeConfig {
  return config;
}
