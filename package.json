{
  "name": "react-native-library",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:native": "turbo dev --filter='@masumdev/*' --filter=native",
    "gen:expo-package": "bunx turbo gen expo-package",
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "bump:patch:rn-ui": "npm version patch --prefix packages/rn-ui --no-git-tag-version",
    "bump:minor:rn-ui": "npm version minor --prefix packages/rn-ui --no-git-tag-version",
    "bump:major:rn-ui": "npm version major --prefix packages/rn-ui --no-git-tag-version",
    "publish:rn-ui": "turbo run build --filter=@masumdev/rn-ui && (cd packages/rn-ui && bun publish --access public)",
    "publish:rn-ui:patch": "npm version patch --prefix packages/rn-ui --no-git-tag-version && turbo run build --filter=@masumdev/rn-ui && (cd packages/rn-ui && bun publish --access public)",
    "publish:rn-ui:minor": "npm version minor --prefix packages/rn-ui --no-git-tag-version && turbo run build --filter=@masumdev/rn-ui && (cd packages/rn-ui && bun publish --access public)",
    "publish:rn-ui:major": "npm version major --prefix packages/rn-ui --no-git-tag-version && turbo run build --filter=@masumdev/rn-ui && (cd packages/rn-ui && bun publish --access public)",
    "publish:all": "turbo run build && bun publish --access public"
  },
  "devDependencies": {
    "@turbo/gen": "^2.9.8",
    "prettier": "^3.1.1",
    "turbo": "^2.10.7"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "react-native-parsed-text": "^0.0.22"
  },
  "overrides": {
    "react-native": "0.86.2",
    "react-native-reanimated": "4.5.1",
    "react-native-gesture-handler": "~2.32.0",
    "react-native-worklets": "0.10.1",
    "react-native-calendars": "^1.1314.0",
    "react-native-svg": "15.15.4",
    "@gorhom/bottom-sheet": "^5.2.14",
    "expo": "^57.0.0",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "@babel/core": "^7.25.0",
    "@babel/preset-env": "^7.25.0"
  }
}
