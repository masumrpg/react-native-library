---
title: Usage
sidebar:
  order: 3
description: Code examples, preset variants, async loading states, and Props API reference for QRCode.
---

import { Code } from '@astrojs/starlight/components';
import TypeTable from '@/components/TypeTable.astro';

export const basicUsageCode = `import React from 'react';
import { QRCode } from '@masumdev/rn-qr-code';

export function BasicQRCodeExample() {
  return (
    <QRCode
      value="https://react-native-library-docs.netlify.app/rn-qr-code/"
      size={200}
      variant="BASIC"
    />
  );
}`;

export const presetVariantCode = `import React from 'react';
import { QRCode } from '@masumdev/rn-qr-code';

export function PresetVariantsExample() {
  return (
    <>
      {/* Heart preset variant */}
      <QRCode value="Love QR" size={180} variant="HEART" />

      {/* Linear Gradient preset variant */}
      <QRCode value="Gradients" size={180} variant="LINEAR_GRADIENT" />

      {/* Triangle preset variant */}
      <QRCode value="Triangle" size={140} variant="TRIANGLE" />
    </>
  );
}`;

export const asyncStateCode = `import React from 'react';
import { QRCode } from '@masumdev/rn-qr-code';
import { Skeleton } from '@masumdev/rn-ui';

export function AsyncQRCodeExample() {
  return (
    <QRCode
      value="https://react-native-library-docs.netlify.app/rn-qr-code/"
      size={180}
      isAsync
      renderLoading={() => (
        <Skeleton style={{ width: 180, height: 180 }} radius="md" />
      )}
    />
  );
}`;

## Basic Usage

Render a vector SVG QR code by passing `value` and `size`:

<Code code={basicUsageCode} lang="tsx" title="BasicQRCodeExample.tsx" mark={['QRCode', 'value', 'size', 'variant="BASIC"']} />

## Preset Design Variants

Use the `variant` prop to apply pre-configured design styles:

<Code code={presetVariantCode} lang="tsx" title="PresetVariantsExample.tsx" mark={['variant="HEART"', 'variant="LINEAR_GRADIENT"', 'variant="TRIANGLE"']} />

## Non-Blocking Async Rendering

Set `isAsync` to generate the QR matrix off the synchronous render path, preventing UI frame drops during screen transitions:

<Code code={asyncStateCode} lang="tsx" title="AsyncQRCodeExample.tsx" mark={['isAsync', 'renderLoading']} />

## Props API

<TypeTable
  type={{
    value: {
      type: 'string',
      description: 'Payload text or URL encoded in the QR code.',
    },
    size: {
      type: 'number',
      default: '250',
      description: 'Width and height of the rendered SVG container in dp.',
    },
    variant: {
      type: 'keyof typeof QR_CODE_CONFIGS',
      default: "'BASIC'",
      description: 'Pre-configured design preset key from QR_CODE_CONFIGS.',
    },
    color: {
      type: 'string',
      default: "'#000000'",
      description: 'Primary foreground color for cells.',
    },
    backgroundColor: {
      type: 'string',
      default: "'transparent'",
      description: 'Background canvas color.',
    },
    piece: {
      type: '{ shape?: "square" | "dot" | "rounded" | "heart" | "triangle" | "rain"; color?: string; size?: number; borderRadius?: number }',
      description: 'Options for individual data module cells.',
    },
    eye: {
      type: 'Partial<{ topLeft: EyeOptions; topRight: EyeOptions; bottomLeft: EyeOptions }>',
      description: 'Shape, color, and size overrides for 3 corner eye patterns.',
    },
    logo: {
      type: 'LogoOptions',
      description: 'Center logo image source, size ratio (0.0-1.0), padding, and background color.',
    },
    gradient: {
      type: 'QRCodeGradientConfig',
      description: 'Linear or radial gradient config with color stops and mask options.',
    },
    imageClip: {
      type: 'ImageProps',
      description: 'Image href background clip fill for data modules.',
    },
    isAsync: {
      type: 'boolean',
      default: 'false',
      description: 'Enables non-blocking asynchronous calculation using setTimeout(..., 0).',
    },
    errorCorrectionLevel: {
      type: "'L' | 'M' | 'Q' | 'H'",
      default: "'M'",
      description: 'QR error correction level (Level H recommended when using center logos).',
    },
    isLoading: {
      type: 'boolean',
      default: 'false',
      description: 'Displays the loading placeholder view.',
    },
    renderLoading: {
      type: '() => ReactNode',
      description: 'Custom loading placeholder component renderer.',
    },
  }}
/>
