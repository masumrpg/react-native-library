---
title: DropdownMenu
description: Contextual dropdown menu items with shortcuts and checkbox menu items.
---

import { Code } from '@astrojs/starlight/components';
import TypeTable from '@/components/TypeTable.astro';

export const exampleCode = `import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, Button } from '@masumdev/rn-ui';

export function DropdownExample() {
  return (
    <DropdownMenu>
      <DropdownMenuTrigger>
        <Button variant="outline">Options Menu</Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem onPress={() => console.log('Edit')}>Edit Profile</DropdownMenuItem>
        <DropdownMenuItem onPress={() => console.log('Settings')}>Settings</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}`;

`DropdownMenu` displays list menus triggered by button actions.

## Usage

<Code code={exampleCode} lang="tsx" title="DropdownExample.tsx" mark={['DropdownMenu', 'DropdownMenuTrigger', 'DropdownMenuContent', 'DropdownMenuItem']} />

## Props API

<TypeTable
  type={{
    children: { type: 'ReactNode', description: 'Trigger and Content menu items.' },
  }}
/>
