---
title: InputGroup
description: Compound input component with prefix/suffix addons, buttons, icons, and text elements.
---

import { Code } from '@astrojs/starlight/components';
import TypeTable from '@/components/TypeTable.astro';

export const exampleCode = `import { InputGroup, InputGroupAddon, InputGroupInput, InputGroupButton } from '@masumdev/rn-ui';

export function InputGroupExample() {
  return (
    <InputGroup>
      <InputGroupAddon>https://</InputGroupAddon>
      <InputGroupInput placeholder="masum.dev" />
      <InputGroupButton tone="primary">Go</InputGroupButton>
    </InputGroup>
  );
}`;

`InputGroup` merges inputs, prefix/suffix labels, and buttons into a single integrated bar.

## Usage

<Code code={exampleCode} lang="tsx" title="InputGroupExample.tsx" mark={['InputGroup', 'InputGroupAddon', 'InputGroupInput', 'InputGroupButton']} />

## Props API

<TypeTable
  type={{
    children: { type: 'ReactNode', description: 'InputGroup sub-components.' },
    orientation: { type: "'horizontal' | 'vertical'", default: "'horizontal'", description: 'Layout direction.' },
  }}
/>
