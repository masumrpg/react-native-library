---
title: Select
description: Themed dropdown selection component for React Native.
---

import { Code } from '@astrojs/starlight/components';
import TypeTable from '@/components/TypeTable.astro';

export const selectCode = `import { Select } from '@masumdev/rn-ui';

<Select
  placeholder="Choose a language"
  options={[
    { label: 'English', value: 'en' },
    { label: 'Bahasa Indonesia', value: 'id' },
    { label: 'Arabic', value: 'ar' },
  ]}
  onValueChange={(val) => console.log(val)}
/>`;

<Code code={selectCode} lang="tsx" title="SelectExample.tsx" mark={['Select', 'options']} />

## Props API

<TypeTable
  type={{
    options: { type: '{ label: string; value: string }[]', description: 'Available select items.' },
    value: { type: 'string', description: 'Selected item value.' },
    placeholder: { type: 'string', description: 'Placeholder label when nothing is selected.' },
    onValueChange: { type: '(value: string) => void', description: 'Callback fired on selection.' },
  }}
/>
