---
title: Select
description: Dropdown select input picker modal with option descriptions and custom icons.
---

import { Code } from '@astrojs/starlight/components';
import TypeTable from '@/components/TypeTable.astro';

export const selectCode = `import { useState } from 'react';
import { Select } from '@masumdev/rn-ui';

export function SelectExample() {
  const [selected, setSelected] = useState('expo');

  return (
    <Select
      title="Choose Framework"
      placeholder="Select mobile framework..."
      value={selected}
      onValueChange={setSelected}
      options={[
        { value: 'expo', label: 'Expo React Native', description: 'Recommended managed workflow.' },
        { value: 'cli', label: 'React Native CLI', description: 'Bare React Native setup.' },
        { value: 'web', label: 'React Native Web', description: 'Cross-platform web target.', disabled: true },
      ]}
    />
  );
}`;

`Select` triggers a modal bottom picker sheet for selecting items from an options list.

## Usage

<Code code={selectCode} lang="tsx" title="SelectExample.tsx" mark={['Select', 'options', 'onValueChange']} />

## Props API

<TypeTable
  type={{
    options: { type: 'SelectOption[]', description: 'Array of option objects with value, label, description, and optional disabled state.' },
    value: { type: 'string', description: 'Currently selected option value.' },
    defaultValue: { type: 'string', description: 'Initial default selected value.' },
    placeholder: { type: 'string', default: "'Select option'", description: 'Placeholder string when no option is selected.' },
    title: { type: 'string', default: "'Select'", description: 'Modal sheet header title string.' },
    onValueChange: { type: '(value: string) => void', description: 'Callback when an option is selected.' },
    chevronIcon: { type: 'RenderIcon', description: 'Custom trailing dropdown arrow icon renderer.' },
    checkIcon: { type: 'RenderIcon', description: 'Custom checkmark icon for active item.' },
  }}
/>
