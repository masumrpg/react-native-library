---
title: Installation
sidebar:
  order: 2
description: How to install @masumdev/rn-ui and configure peer dependencies in React Native or Expo apps.
---

import { Tabs, TabItem, Code } from '@astrojs/starlight/components';
import Callout from '@/components/Callout.astro';

export const npmInstall = `npm i @masumdev/rn-ui react-native-reanimated react-native-gesture-handler react-native-worklets react-native-safe-area-context`;
export const pnpmInstall = `pnpm add @masumdev/rn-ui react-native-reanimated react-native-gesture-handler react-native-worklets react-native-safe-area-context`;
export const yarnInstall = `yarn add @masumdev/rn-ui react-native-reanimated react-native-gesture-handler react-native-worklets react-native-safe-area-context`;
export const bunInstall = `bun add @masumdev/rn-ui react-native-reanimated react-native-gesture-handler react-native-worklets react-native-safe-area-context`;

export const npmOptional = `npm i @gorhom/bottom-sheet react-native-calendars expo-secure-store`;
export const pnpmOptional = `pnpm add @gorhom/bottom-sheet react-native-calendars expo-secure-store`;
export const yarnOptional = `yarn add @gorhom/bottom-sheet react-native-calendars expo-secure-store`;
export const bunOptional = `bun add @gorhom/bottom-sheet react-native-calendars expo-secure-store`;

export const babelCode = `module.exports = {
  presets: ['babel-preset-expo'],
  plugins: [
    // Reanimated plugin must be listed last
    'react-native-reanimated/plugin',
  ],
};`;

export const rootLayoutCode = `import React from 'react';
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import { ThemeProvider, ToastProvider } from '@masumdev/rn-ui';
import * as SecureStore from 'expo-secure-store';
import { Stack } from 'expo-router';

const themeStorage = {
  getItem: (key: string) => SecureStore.getItemAsync(key),
  setItem: (key: string, value: string) => SecureStore.setItemAsync(key, value),
};

export default function RootLayout() {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <ThemeProvider
        defaultColorScheme="system"
        storage={themeStorage}
        storageKey="app-color-scheme"
      >
        <ToastProvider placement="bottom">
          <Stack screenOptions={{ headerShown: false }} />
        </ToastProvider>
      </ThemeProvider>
    </GestureHandlerRootView>
  );
}`;

## Step 1: Install Package

Install `@masumdev/rn-ui` along with required peer dependencies using your preferred package manager:

<Tabs syncKey="pkg">
  <TabItem label="npm" icon="seti:npm">
    <Code code={npmInstall} lang="bash" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code={pnpmInstall} lang="bash" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code={yarnInstall} lang="bash" />
  </TabItem>
  <TabItem label="bun" icon="bun">
    <Code code={bunInstall} lang="bash" />
  </TabItem>
</Tabs>

If you plan to use `BottomSheet`, `Calendar`, or persistent theme storage (`SecureStore`), install optional dependencies:

<Tabs syncKey="pkg">
  <TabItem label="npm" icon="seti:npm">
    <Code code={npmOptional} lang="bash" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code={pnpmOptional} lang="bash" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code={yarnOptional} lang="bash" />
  </TabItem>
  <TabItem label="bun" icon="bun">
    <Code code={bunOptional} lang="bash" />
  </TabItem>
</Tabs>

## Step 2: Configure Babel Plugin

Add `react-native-reanimated/plugin` to `babel.config.js`:

<Code code={babelCode} lang="js" title="babel.config.js" mark={['react-native-reanimated/plugin']} />

<Callout type="info">
Reanimated plugin must always be placed as the last plugin in your `babel.config.js`.
</Callout>

## Step 3: Set Up Root Providers

Wrap your root navigation container with `GestureHandlerRootView`, `ThemeProvider`, and `ToastProvider`:

<Code code={rootLayoutCode} lang="tsx" title="_layout.tsx" mark={['GestureHandlerRootView', 'ThemeProvider', 'ToastProvider']} />
